<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fadilayona.griyabusanawanita">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LayarDetail"
            android:label="Detail Transaksi" />
        <activity
            android:name=".LayarListBaju"
            android:label="List Baju" />
        <activity
            android:name=".LayarInsertBaju"
            android:label="Insert Baju" />
        <activity
            android:name=".LayarEditBaju"
            android:label="Edit Baju" />
        <activity
            android:name=".OpsiMenu" />
    </application>

</manifest>